# Dependencies are declared in pyproject.toml now. Install the package instead:
#   pip install -e .            # runtime only
#   pip install -e ".[dev]"     # + pytest/responses/mypy/ruff
#   pip install -e ".[docx,screenshot]"   # optional converters
-e .[dev]
