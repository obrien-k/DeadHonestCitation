# Example source list for index.py
#
# Copy this to urls.txt (the default, gitignored) and edit, or pass any path:
#   python index.py urls.example.txt
#
# One source per line. Blank lines and lines starting with '#' are ignored.
# Each line is classified on its own, so kinds can be mixed freely:
#   - a Wayback Machine or live URL   (http/https)
#   - a saved page                    (.html / .htm)
#   - a Word document                 (.docx)
#   - a directory                     (every .html/.htm/.docx inside; add --recursive to descend)
# Platform is auto-detected per source; force one with --ghost / --wordpress / --html / --docx.

# A Wayback Machine capture (WordPress / yaaburnee theme):
https://web.archive.org/web/20180811200251/https://example.com/a-blog-post/

# A live page (Ghost):
https://example.com/another-post/

# A saved page on disk (images come from its sibling ./another-post_files/ folder):
./saved-pages/another-post.html

# A Word document:
./drafts/my-draft.docx
